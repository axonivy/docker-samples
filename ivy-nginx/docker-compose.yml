version: '3.3'
services:
  ivy:
    image: "axonivy/axonivy-engine:dev"
    ports:
     - "8080:8080"
    volumes:
     - "./ivy.yaml:/opt/ivy/configuration/ivy.yaml"
  frontend:
    image: "nginx"
    ports:
     - "80:80"
    volumes:
     - "./ivy:/etc/nginx/sites-enabled/ivy"
