#Run this script with shell. The UI export will not export users, but this command will do it.
#It runs into a JBoss instance, but on boot it will do the export. Just CTRL-C to close it after boot up.
/opt/jboss/keycloak/bin/standalone.sh -Djboss.socket.binding.port-offset=100 -Dkeycloak.migration.action=export -Dkeycloak.migration.provider=singleFile -Dkeycloak.migration.realmName=ivy-demo -Dkeycloak.migration.usersExportStrategy=REALM_FILE -Dkeycloak.migration.file=/shared/realm-ivydemo-import.json