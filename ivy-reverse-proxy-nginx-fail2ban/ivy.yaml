# yaml-language-server: $schema=https://json-schema.axonivy.com/ivy/13.2.9/ivy.json
# Axon Ivy Engine must be able to generate absolute URLs, for example in mails.
# For this, the base URL must be configured.
BaseUrl: http://localhost
